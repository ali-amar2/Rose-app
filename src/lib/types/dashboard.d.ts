declare type SidebarDropdownProps = {
  user?: user;
};
declare type user = {
  email?: string;
  firstName?: string;
  lastName?: string;
  photo?: string;
};

declare type UserAvatarProps = {
  user?: user;
};
